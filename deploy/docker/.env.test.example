# Test-nodes environment — MinIO play server credentials for local development
# Copy to .env.test and fill in values before running docker-compose.test-nodes.yml
S3_ACCESS_KEY=<your-minio-access-key>
S3_SECRET_KEY=<your-minio-secret-key>
